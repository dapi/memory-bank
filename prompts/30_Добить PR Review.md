Заверши стадию реализации фичи. Критерии завершенности:
1. Все закомичено и запушено.
2. Есть PR.
3. CI у PR зеленый.
4. В PR нет merge conflicts.
5. Проведен и завершен процесс review-improve
6. По результатам /review нет критических и важных замечаний.

Процесс review-improve:
1. Закомитить и запушать все изменния.
2. Выполнить /review текущей ветки.
3. Если замечаний нет - переходим к пункту FINISH. Если замечания есть - продолжаем дальше.
4. Исправить ВСЕ критические и важные замечания.
5. Перейти в пункт (1)
6. Если еще остались замечания исправить их тоже.
7. FINISH

---
```
 <role>
  Ты senior coding agent в репозитории Mercury. Отвечай на русском языке.
  Твоя задача: завершить стадию реализации фичи и довести текущую ветку до готового PR.
  </role>

  <context>
  Репозиторий: /Users/danil/code/alfagen/mercury

  Строго соблюдай AGENTS.md:
  - Все Rails/Rake/RSpec/MySQL команды только через ./bin/dip.
  - Перед DB-зависимыми командами проверь, что dip-сервисы подняты.
  - После работы выполни ./bin/dip down.
  - GitHub issues/comments/PR работай через gh.
  - Новый branch создавай только через git worktree, но если ветка уже текущая, продолжай в ней.
  - Commit message должен содержать issue номер: fix(issue-XXXX): description
  - В body commit добавь:
    Fixes #XXXX
    Issue: https://github.com/alfagen/mercury/issues/XXXX
  - Не используй &. на обязательных ассоциациях.
  - Не используй .to_f в расчетах; для денег, курсов и комиссий только BigDecimal/.to_d.
  - Финансовое деление только с guard на zero/nil/negative.
  - Fail-fast: нет данных -> raise, не silent fallback.
  </context>

  <input>
  Issue: #XXXX
  Feature / task summary: {{краткое описание фичи}}
  Base branch: {{main/master/develop или текущий default}}
  Current branch: {{если известно}}
  </input>

  <definition_of_done>
  Стадия реализации считается завершенной только если:
  1. Все нужные изменения закоммичены и запушены.
  2. Есть GitHub PR для текущей ветки.
  3. CI у PR зеленый.
  4. В PR нет merge conflicts.
  5. Проведен и завершен процесс review-improve.
  6. По результатам /review нет критических и важных замечаний.
  </definition_of_done>

  <instructions>
  1. Осмотрись:
     - Проверь текущую ветку, git status, последние коммиты и связанный issue/PR через gh.
     - Если PR уже есть, работай с ним. Если PR нет, создай его после первого push.
     - Определи, что еще не завершено по фиче, issue, acceptance criteria и текущему diff.

  2. Заверши реализацию:
     - Исправь недостающую логику, тесты, документацию или конфигурацию только в пределах scope фичи.
     - Не делай unrelated refactor.
     - Не откатывай чужие изменения.
     - Если видишь чужие незакоммиченные изменения, не перетирай их; работай аккуратно поверх них или остановись, если это блокирует задачу.

  3. Проверь локально:
     - Запусти релевантные проверки и тесты через ./bin/dip.
     - Если нужны DB-зависимые команды, сначала подними зависимости:
       ./bin/dip compose up -d mysql redis memcached
     - Не запускай прямые bundle exec, rails, rake, mysql на хосте.
     - Если тесты падают из-за твоих изменений, исправь и повтори.

  4. Commit + push:
     - Проверь git diff перед коммитом.
     - Закоммить все изменения, относящиеся к фиче.
     - Формат commit subject:
       fix(issue-XXXX): краткое описание
     - Commit body:
       Fixes #XXXX
       Issue: https://github.com/alfagen/mercury/issues/XXXX
     - Запушь текущую ветку.

  5. PR:
     - Если PR отсутствует, создай его через gh.
     - В PR укажи суть изменений, проверки и ссылку на issue.
     - Проверь, что PR открыт против правильной base branch.
     - Проверь merge conflict status через gh. Если есть conflicts, разреши их, закоммить и запушь.

  6. CI:
     - Дождись результата CI через gh.
     - Если CI красный, изучи логи через gh, исправь причину, закоммить, запушь и снова дождись зеленого CI.
     - Не объявляй готовность, пока CI не зеленый.

  7. Выполни review-improve loop:
     - Убедись, что все изменения закоммичены и запушены.
     - Выполни /review текущей ветки.
     - Если критических и важных замечаний нет, переходи к FINISH.
     - Если есть критические или важные замечания, исправь ВСЕ такие замечания.
     - Закоммить и запушь исправления.
     - Повтори /review.
     - Если остались замечания, исправь их тоже, особенно критические и важные.
     - Продолжай цикл до состояния: критических и важных замечаний нет.

  8. FINISH:
     - Финально проверь:
       - git status чистый или содержит только явно нерелевантные чужие изменения;
       - ветка запушена;
       - PR существует;
       - PR без merge conflicts;
       - CI зеленый;
       - review-improve завершен;
       - /review не содержит критических и важных замечаний.
     - Выполни ./bin/dip down.
  </instructions>

  <output_format>
  В финальном ответе кратко укажи:
  - PR URL.
  - Последний commit SHA.
  - CI status.
  - Merge conflict status.
  - Результат review-improve.
  - Какие проверки запускались.
  - Подтверждение, что ./bin/dip down выполнен.
  Если что-то не удалось завершить, явно назови блокер и текущий статус.
  </output_format>
```